---
import Layout from '../../layouts/Layout.astro';
import PublicNetworkUpgradePage from '../../components/PublicNetworkUpgradePage';
import { getUpgradeById } from '../../data/upgrades';

const upgrade = getUpgradeById('pectra')!;
---

<Layout
  title="Pectra Upgrade - Forkcast"
  description="Account abstraction, validator upgrades, and 2x blob throughput - making Ethereum faster and cheaper. Live on mainnet May 7, 2025."
>
  <PublicNetworkUpgradePage
    client:only="react"
    forkName="Pectra"
    displayName={upgrade.name}
    description={upgrade.description}
    status={upgrade.status}
    activationDate={upgrade.activationDate}
    metaEipLink={upgrade.metaEipLink}
    activationDetails={upgrade.activationDetails}
  />
</Layout>
